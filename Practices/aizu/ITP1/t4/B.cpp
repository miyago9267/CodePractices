// By myg9267
// i m bad in coding, so sad
// it a me mario
#include <bits/stdc++.h>
#define endl "\n"
#define IO ios::sync_with_stdio(0), cin.tie(0), cout.tie(0)
#define MAXN maxn
#define MOD modn
#define ll long long

using namespace std;

signed main(){
    //IO;
    //freopen("p.in", "r", stdin);
    //freopen("p.out", "w", stdout);
    double r;
    const double pi = M_PI;
    while (cin>>r) {
        cout<<setprecision(6)<<fixed<<r*r*pi<<' ';
        cout<<setprecision(6)<<fixed<<r*2*pi<<endl;
    }
    return EXIT_SUCCESS;
}